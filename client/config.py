# =========================================================
# 客户端配置
# =========================================================
# 客户端只保留接口访问能力，不保存数据集、不保存模型、不运行 YOLO。
# 部署后把 SERVER_URL 改成你的云服务器公网地址，把 API_KEY 改成服务器 .env 中的值。
# =========================================================

SERVER_URL = "http://154.8.177.102:8000"
API_KEY = "change-this-long-random-key"

# 检测接口可能较慢，超时时间设置长一点。
REQUEST_TIMEOUT = 600
